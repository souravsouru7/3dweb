{
    "git": {
        "deploymentEnabled": {
            "main": true
        }
    },
    "github": {
        "silent": true
    },
    "buildCommand": "npm run build",
    "installCommand": "git lfs install && git lfs pull && npm install"
}